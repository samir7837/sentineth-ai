# Sentineth AI - development / test dependencies
#
# Install on top of the runtime requirements:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest==8.4.2
ruff==0.16.5
mypy==2.3.1
